const age: number = 10;

// type npx tsc my-file.ts in order to compile the file
// this will create a my-file.js file
// we can add the --watch flag to watch for changes
// npx tsc my-file.ts --watch
// we can also add the --outFile flag to compile multiple files into one
// we can also add the --removeComments flag to remove comments from the output
// npx tsc my-file.ts --outFile my-file.js --removeComments
// we can also add es6 as the target to compile to es6
// npx tsc my-file.ts --target es6
